import lostAndFoundService, { MissingItemReport, FoundItem } from 'services/lostAndFound';
import { Card, Button, Grid, Typography } from '@mui/material';

const detailsCard = (detailsType: string) => {
  <Card>
    <Grid>
      <Grid>
        <Typography>9/5/24</Typography>
      </Grid>
    </Grid>
  </Card>;
};
